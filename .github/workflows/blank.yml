name: Deployment
on: workflow_dispatch

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: deploy
        run: |
          sleep 5000
          echo "Deploying to production"
          
